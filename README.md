# Photo Gallery (Spring Boot)

(WORK IN PROGRESS)

* *used:* **Spring Boot** framework, **Tomcat** embedded server, **Maven** wrapper
* *Spring Boot dependencies:* Spring Web, Spring Data JDBC, H2 Database